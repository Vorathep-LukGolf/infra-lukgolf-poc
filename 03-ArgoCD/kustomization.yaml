apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - 01-namespace.yaml
  - https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
  - 04-argocd-cmd-parms-cm.yaml

patches:
  - target:
      kind: Service
      name: argocd-server
    path: 03-server-nodeport-patch.yaml